- name: reload systemd
  ansible.builtin.systemd_service: { daemon_reload: true }

- name: restart ferrite-worker
  ansible.builtin.systemd_service:
    name: "ferrite-worker@{{ item }}"
    state: restarted
  loop: "{{ range(0, ferrite_cpu_groups) | list }}"
