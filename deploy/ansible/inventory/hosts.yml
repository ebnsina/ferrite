# Rent a machine, add it here, run the playbook.
all:
  children:
    control_plane:
      hosts:
        cp-1: { ansible_host: 10.0.0.10 }
    workers:
      hosts:
        worker-1: { ansible_host: 10.0.1.10, verve_cpu_groups: 4 }
